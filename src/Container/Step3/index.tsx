import { ReactNode } from 'react'

type Step3ContainerProps = {
  children: ReactNode
}

export const Step3Container = ({ children }: Step3ContainerProps) => {
  // Context providers and business logic will be added here
  return children
}

export default Step3Container
