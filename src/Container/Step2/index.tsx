import { ReactNode } from 'react'

type Step2ContainerProps = {
  children: ReactNode
}

export const Step2Container = ({ children }: Step2ContainerProps) => {
  // Context providers and business logic will be added here
  return children
}

export default Step2Container
