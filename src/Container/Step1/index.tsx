import { ReactNode } from 'react'

type Step1ContainerProps = {
  children: ReactNode
}

export const Step1Container = ({ children }: Step1ContainerProps) => {
  // Context providers and business logic will be added here
  return children
}

export default Step1Container
